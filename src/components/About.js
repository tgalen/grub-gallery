const ABOUT_STYLE = {
  height: "80vh",
  width: "90%",
  border: "5px solid",
  margin: "auto",
  borderRadius: "10px",
  textAlign: "center",
};

const About = () => {
  return (
    <div style={ABOUT_STYLE}>
      <p>This is about the site</p>
    </div>
  );
};

export default About;
